# Story 6.2 — Cut over `notes/note_generator.py` to `llm.client`

- **Status:** Draft
- **Epic:** [EPIC-INTEGRATION-CUTOVER](../epic.md)
- **Depends on:** 6.1 (regression corpus committed); EPIC-CHIRPD-CORE 3.6 (`client.chat` streaming + non-streaming surface available); EPIC-CHIRPD-CORE 3.4 (`llm.client` with lazy-spawn + version-mismatch handling); EPIC-MODEL-REGISTRY (a chat model registered as `default_chat` so the daemon can resolve `"default"`)
- **Blocks:** 6.5 (fixture migration), 6.6 (regression run)

## Prerequisites

- Story 6.1 is merged with ≥10 baselines committed under `tests/regression/notes_quality/`. Without this, the cutover removes the Ollama call site whose output the corpus was captured against — making the comparison in story 6.6 impossible to reproduce.
- A chat-role model is registered in `models.toml` via `chirp models add <chat-repo>` and is the `default_chat`. The daemon resolves `"default"` to this alias per FR35.
- `chirpd` runs (either via lazy-spawn from the first client call, or already running under LaunchAgent if EPIC-DAEMON-LIFECYCLE has shipped that).

## User story

As a chirp user running `chirp transcribe`, I want the note-generation step to route its LLM call through `chirpd` via `llm.client` instead of the Ollama HTTP API, so that note generation works without a separately-installed Ollama daemon — while my notes continue to follow the same XML structure, prompt template, and front-matter format.

## Context

`notes/note_generator.py` is the chirp module that turns a Whisper transcript into a structured `notes.md` file during the `chirp transcribe` flow. The current implementation:

- Holds the `SYSTEM_PROMPT` block (XML output contract) at module top (lines 20–136). **This stays put** — prompt assembly is owned by chirp per FR48.
- Builds the prompt in `_generate_structured_notes` (lines 329–353) by concatenating `SYSTEM_PROMPT`, a `title_instruction`, and the transcript.
- Sends the prompt to Ollama via `_call_ollama` (lines 355–401) using `requests.post(f"{ollama_url}/api/generate", ...)` with `stream=True`, accumulating tokens by iterating `response.iter_lines()`.
- Parses the streamed-then-joined response as XML via `_parse_xml_response` (lines 403–474). **This stays put** — parsing is owned by chirp.
- Writes the rendered note via `template_engine.render_meeting_section(...)` and front-matter helpers (lines 280–328). **These stay put.**

This story replaces **only** `_call_ollama` (and its `requests` / `json` / `ollama_url` / `num_predict` dependencies) with a call to `llm.client.chat(...)`. Everything else in this module is preserved verbatim.

The new call site uses non-streaming chat for note generation. Note generation is a one-shot batch task — there is no interactive UX where streaming tokens would be useful. The current Ollama path streams internally only to print a "↳ Generating... (N chunks)" progress message every 20 tokens; we preserve that progress UX by switching to `stream=True` on the client side and aggregating tokens locally before XML-parsing the joined result. The user-visible behavior (progress dots while generation proceeds, then the parsed note written to disk) is unchanged.

The Ollama path produces a single prompt string. The `chirp.llm` client takes a `messages: list[dict]` shape. We map the existing single-prompt usage to a one-message list: `[{"role": "user", "content": <full_prompt>}]`. The model's tokenizer applies its chat template at the daemon side per FR8. This is the simplest faithful mapping and preserves prompt content verbatim — the system prompt block remains part of the user message, exactly as today.

## Acceptance criteria

- **AC-1** `notes/note_generator.py` no longer imports `requests`. The `import requests` line at L8 is removed. The `import json` at L366 (inside `_call_ollama`) is also removed; it was only used to parse the Ollama NDJSON stream.
- **AC-2** The `_call_ollama(prompt: str) -> str` method (current L355–401) is replaced by a new method `_call_llm(prompt: str) -> str` with the same signature. The body uses `llm.client`:
  - Instantiate or reuse a module-level client: `from llm.client import client` (or whatever shape `llm.client` exposes per EPIC-CHIRPD-CORE story 3.4 — match the documented public API).
  - Call `client.chat(model="default", messages=[{"role": "user", "content": prompt}], options={"temperature": 0.3, "top_p": 0.9, "max_tokens": <num_predict>}, stream=True)`.
  - Iterate the returned stream, accumulating each delta's `.text` into a list. Every 20 deltas, render the same `[dim]  ↳ Generating... (N chunks)[/dim]` progress line on `self.console` that the current implementation produces (current L389–392).
  - After the stream completes, render the same `[dim]  ↳ Generated N chunks[/dim]` summary (current L398–399) on `self.console`.
  - Return `"".join(deltas).strip()`.
- **AC-3** `_generate_structured_notes` (current L329–353) is updated to call `self._call_llm(prompt)` instead of `self._call_ollama(prompt)`. No other lines in this method change. The prompt-construction step (system prompt + title instruction + transcript) is unchanged.
- **AC-4** `self.settings.models.ollama_url` and `self.settings.models.num_predict` are **no longer read** inside `_call_llm`. The old `num_predict` value is passed through as `max_tokens` in the `options` dict on `client.chat` — this preserves the existing "how many tokens may we generate" budget. `ollama_url` simply has no equivalent and is dropped. (The Pydantic settings fields `ollama_url` and `num_predict` are not removed by this story — they may still be read by other modules; the field cleanup is owned by EPIC-INIT-AND-MIGRATION.)
- **AC-5** Exception handling mirrors the existing best-effort posture. The outer `try/except Exception` in `_generate_structured_notes` (current L348–353) is preserved: any `LLMError` (transport / protocol / model) raised by `client.chat` propagates up and is caught the same way `requests`-level errors were caught today. The downstream behavior (log at debug, return `None`, write nothing to disk for that record) is unchanged. No new user-facing prompts or recovery paths are added in this story.
- **AC-6** XML parsing (`_parse_xml_response`, `_parse_fallback_response`) is untouched. The `SYSTEM_PROMPT` constant is untouched. The template engine call (`self.template_engine.render_meeting_section(...)`) is untouched. The `_update_meta` method is untouched (`whisper_model` and `llm_model` fields in `meta.toml` still record what the settings layer reports; the value of `settings.models.llm` is now an alias-into-`models.toml` rather than an Ollama tag — that semantic shift is invisible to this module).
- **AC-7** `rg -n "ollama|requests\.|/api/generate|num_predict|ollama_url" notes/note_generator.py` returns zero matches.
- **AC-8** Existing tests under `tests/notes/` that exercise note generation pass after the cutover, with their fixtures temporarily continuing to mock at the `notes/note_generator.NoteGenerator._call_llm` boundary (or at `llm.client.chat`, implementer's choice — whichever is the smaller diff). Full fixture migration to `FakeBackend` is owned by story 6.5; this story commits a working state but does not need to write the canonical fixture pattern.
- **AC-9** A new unit test `tests/notes/test_note_generator_llm_call.py` (or an addition to an existing `test_note_generator.py`) asserts:
  - `_call_llm` calls `client.chat` with `model="default"`, with `messages` containing a single user-role entry whose `content` starts with the `SYSTEM_PROMPT` constant.
  - `_call_llm` passes `temperature=0.3`, `top_p=0.9`, and `max_tokens` equal to `settings.models.num_predict` inside `options`.
  - `_call_llm` returns the joined token stream stripped of leading/trailing whitespace.
  - When the mocked `client.chat` raises `LLMModelError`, `_generate_structured_notes` catches it via its outer `except Exception` and returns `None`.
- **AC-10** `make check` and `make test` pass after the cutover. Coverage on `notes/note_generator.py` does not regress vs. the pre-cutover baseline (NFR-M1).
- **AC-11** Manual smoke test: with `chirpd` running and a default chat model registered, run `chirp transcribe` against one of the recordings in `tests/regression/notes_quality/<slug>/` (or any fresh recording). Confirm `~/Documents/chirp/<slug>/notes.md` is generated, has the expected `---` front-matter block, contains the structured sections (`## Executive Summary`, `## Action Items`, etc.), and that `meta.toml` records `llm_model` (the value resolved by settings — alias name post-migration).

## Implementation tasks

1. **Read the `llm.client` public API.** Cross-reference EPIC-CHIRPD-CORE story 3.4 (`client.chat`/`client.embed` shape, lazy-spawn semantics) and story 3.6 (streaming iterator shape). Confirm the canonical import path (likely `from llm.client import client` or `from llm import client`) and the streaming-delta object shape (`Delta` or similar). Match exactly what the client module exposes.

2. **Replace `_call_ollama` with `_call_llm`.** Inside `notes/note_generator.py`:
   - Remove `import requests` from the top.
   - Replace the entire body of the renamed method per AC-2. Keep the `self.console.print(...)` progress lines verbatim — the `[dim]  ↳ Generating... ({N} chunks)[/dim]` and `[dim]  ↳ Generated N chunks[/dim]` outputs are familiar UX and load-bearing for the "transcribe is doing something" feel.
   - Remove the inline `import json` from inside the old method (no longer needed; the daemon emits typed deltas, not JSON lines that we parse client-side).

3. **Update `_generate_structured_notes` to call `_call_llm`.** Change the single call site at current L349. Verify the surrounding `try/except Exception` still wraps the LLM call and the XML parse.

4. **Decide whether to keep the `temperature` / `top_p` literals or thread them through settings.** Today they are literals in `_call_ollama` at L363. Preserve them as literals in `_call_llm` for this story — making them configurable is a separate concern out of this epic's scope. (Note: this matches the existing code's posture; no behavior change.)

5. **Adapt streaming token accumulation.** The current Ollama loop counts each NDJSON line as one "chunk." The new code counts each `Delta` from `client.chat(stream=True)` as one "chunk" — semantically equivalent enough for the progress line (the progress UX is informational, not load-bearing). Use the same `chunk_count % 20 == 0` cadence for the progress reprint.

6. **Update unit tests.** Author / update `tests/notes/test_note_generator_llm_call.py` per AC-9. Mock `client.chat` at its import point inside `notes/note_generator.py` (e.g., `monkeypatch.setattr("notes.note_generator.client.chat", fake_chat)`). The fake should return a generator yielding objects with a `.text` attribute. Assert: model, messages, options, return value, exception propagation.

7. **Verify gates.** Run `make check`, `make test`. Run `rg -n "ollama|requests\.|num_predict|ollama_url" notes/note_generator.py` and confirm zero matches.

8. **Manual smoke.** Run `chirpd` (if not under LaunchAgent), confirm `chirp models list` shows a `default_chat` model, run `chirp record "smoke note"` followed by `chirp transcribe`, inspect the resulting `~/Documents/chirp/<slug>/notes.md`.

## Dev notes

- **Why non-streaming output to the user, but streaming under the hood.** Note generation produces a single artifact (one `notes.md` file). The user is not reading tokens one at a time — they are watching a progress indicator. Streaming on the client side gives us the same progress UX as today; the parsed output is the joined result regardless. We avoid `stream=False` because the daemon may have higher first-token latency in non-streaming mode and the existing `[dim]  ↳ Generating...[/dim]` feedback loop is part of the UX.
- **Why a single user-role message.** The current Ollama call uses `/api/generate` with a single `prompt` string. Mapping that to `messages=[{"role": "user", "content": prompt}]` is the most faithful translation. Splitting `SYSTEM_PROMPT` into a separate system message (`messages=[{"role":"system","content":SYSTEM_PROMPT}, {"role":"user","content":transcript_block}]`) could in principle be cleaner, but it would change how the model's chat template handles the prompt — and the regression corpus's "before" baseline was captured against the single-prompt path. Preserving single-user-message shape keeps the comparison in story 6.6 honest. If a future change wants to split system / user roles, it lives outside this epic.
- **Why pass `max_tokens` rather than `num_predict`.** The `chirp.llm` client's `options` dict uses the `max_tokens` field name per architecture §Wire Protocol Naming Conventions. The semantic is identical to Ollama's `num_predict` — both cap generation length. The settings field name (`num_predict`) stays in `config/settings.py` and is renamed (or removed) by EPIC-INIT-AND-MIGRATION as part of the broader settings sweep.
- **Why not catch `LLMError` explicitly.** The existing code catches the broad `except Exception` and treats any LLM-side failure as "structured-notes extraction failed; return `None`." Replacing the catch with a typed `except (LLMTransportError, LLMProtocolError, LLMModelError)` would add precision but change the recovery surface. Out of scope for this story. The typed-catch refactor is a candidate follow-up but is not required by the epic — the existing best-effort posture is preserved verbatim.
- **Why `model="default"`.** Per FR35 the literal `"default"` resolves to `default_chat` for chat ops. This keeps the call site decoupled from any specific alias name and lets the user swap models freely with `chirp models default <alias>` without re-touching the code.
- **Why no per-call timeout argument.** The daemon's streaming chat does not have an outer HTTP-style timeout — it streams as long as the model produces tokens (bounded by `max_tokens`). The current 300 s `requests` timeout disappears with no replacement. If a model hangs mid-generation, `client.cancel(req_id)` is the escape; for one-shot note generation this is acceptable because the user can Ctrl-C the chirp CLI process, which would close the connection and cause the daemon to treat the connection-close as a cancel.
- **Why the progress UX is preserved.** The `[dim]  ↳ Generating... ({N} chunks)[/dim]` rolling line is genuine UX: it tells the user that note generation is making progress on a long transcript. Removing it would feel like the CLI hung. Keeping it costs a few lines of code and one variable.
- **`make check` boundary.** This is one of three independent cutover stories (6.2 / 6.3 / 6.4). Land them in any order — each must leave `make check` and `make test` green on its own. If story 6.3 lands first, this story's tests should not depend on it. If this story lands first, tests in `tests/notes_chat/` continue to use whatever Ollama mocks they currently use.
- **Fixture migration deferred to 6.5.** This story's tests are allowed to mock either `client.chat` (the new path) at its call site inside `notes/note_generator.py`, **or** to mock `requests.post` (if any tests retain the old fixture shape during the transition). Story 6.5 retires the old fixture shape across the suite — do not pre-emptively refactor every test in this story.

## Testing

- **Unit:** `tests/notes/test_note_generator_llm_call.py` per AC-9. Mock `client.chat` to return a fake generator yielding deltas with `.text` strings. Assert call-shape, options, return value, and exception propagation. Avoid mocking `mlx_lm.*` directly (anti-pattern per architecture).
- **Unit:** any existing `tests/notes/test_note_generator.py` cases that exercise the note-generation flow pass after the cutover. If they relied on patching `requests.post`, replace with patching `client.chat` for this story (transitional fixture; final fixture pattern lands in 6.5).
- **Integration (manual):** end-to-end `chirp transcribe` on a recording from the regression corpus. The output `notes.md` parses as expected (front-matter present, sections present, no XML-tag bleed-through). Compare informally to the `notes_before.md` for the same slug — large differences are expected (different model class), but the structural shape should match.
- **Negative path (manual):** stop `chirpd` (e.g., `pkill -f chirpd`) and run `chirp transcribe`. Expect a clear error from the client's `LLMDaemonUnreachable` (or the broader `LLMTransportError`), surfaced through the existing best-effort `except Exception` to a `notes.md` not generated for that record + a debug log entry. The CLI exits with the existing failure code for `notes/__init__.py`'s wrapper command. Then restart the daemon and re-run; the transcribe completes.
